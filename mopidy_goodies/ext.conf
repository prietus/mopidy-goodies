[goodies]
enabled = true
# Path to a named pipe the operator wires up in [audio] output (tee →
# filesink). When set, /goodies/audio/visualizer (WebSocket) streams raw
# PCM chunks from it. Leave empty to disable the endpoint.
visualizer_fifo =
